import 'package:flutter/material.dart';

class CustomLoadingInd extends StatelessWidget {
  const CustomLoadingInd({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
